[build-system]
requires = ["setuptools>=42", "wheel", "scikit-build", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "solvers"
version = "0.1.0"
description = "Quantum solvers with pybind11"
authors = [{name="You"}]
requires-python = ">=3.8"